# 快速开始

目标：用最少步骤把文档站跑起来，确认你后续只需要写 Markdown 就能维护所有教程。

## 本地预览

在仓库根目录执行：

```bash
python3 -m pip install -U mkdocs-material
python3 -m mkdocs serve
```

然后打开终端提示的本地地址预览。

## 目录结构（你只需要记住两点）

- 文档内容都在 `docs/`
- 侧边栏导航在 `mkdocs.yml` 的 `nav:` 配置里

下一步建议先把「ccswitch 整合」这栏的 Step 页面内容填起来，形成你自己的写作模板。
